import { MappingProfile } from '@automapper/types';

// all commented code below are new propeties from the entities in the server
// that is not yet mapped in the client side
export const resourcesProfile: MappingProfile = (mapper) => {

};
